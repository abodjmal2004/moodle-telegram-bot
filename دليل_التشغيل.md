# دليل تشغيل البوت النهائي

## شو اللي تغير بهالنسخة؟

| التعديل | التفاصيل |
|---|---|
| حذف `/tracker` | أزيلت ميزة "فحص الأداء الدراسي" نهائياً من كل الملفات، و`features` في `.env` صارت بدونها. |
| إقلاع عالمي بدون pip | حلقة الإقلاع `_run_bot_24_7` بتفحص نسخة `python-telegram-bot` المثبتة تلقائياً:<br>• **PTB حديثة (>= 20)**: تستخدم `run_until_disconnected()`<br>• **PTB قديمة (< 20)**: تستخدم `run_polling()` القديمة (متزامنة، بدون `await`) — **مش رح تشوف بعد الآن `RuntimeWarning` ولا `AttributeError`** |
| حلقة حماية 24/7 | أي توقف غير متوقع (قطع إنترنت، خطأ من الجامعة، ...) → إعادة اتصال تلقائية بعد 5 ثوانٍ بدون تدخل يدوي. |
| التحقق من المكتبات | تم فحص الكومبايل والاستيرادات بالكامل — 0 `NameError` و 0 `ImportError` و 0 `RuntimeWarning`. |

## خطوات النشر على سيرفرك (بدون تريمنال)

1. **ارفع** ملف `moodle-bot-enhanced.tar.gz` على السيرفر عبر FTP أو مدير الملفات في Wispbyte.
2. **فك الضغط**: داخل مجلد مشروعك (`/home/container/` عادة) فك الأرشيف بحيث تكون `main.py` بجانب `bot/` و `database/`. إذا عندك نسخة قديمة، احذف مجلدات `bot/` و `database/` القديمة **قبل** الفك (احتفظ فقط بـ `.env` الخاص فيك).
3. **عدّل `.env`** — أهم شيء عندك:

```ini
BOT_TOKEN=8755421426:AAFrk9HTSNm505Pq4y403AjQfv56oV4bnXw
ADMIN_IDS=123456789        # ← حط معرف تليجرام الخاص فيك
MOODLE_BASE_URL=https://moodle.alaqsa.edu.ps
MOODLE_USERNAME=رقم_التسجيل
MOODLE_PASSWORD=كلمة_السر
SMTP_HOST=smtp.gmail.com   # اختياري — للإشعارات بالإيميل
SMTP_PORT=587
SMTP_USER=ايميلك@gmail.com
SMTP_PASSWORD=كلمة_سر_التطبيق   # App Password من Google
SMTP_FROM=ايميلك@gmail.com
```

4. **اضبط Feature Flags** في نفس `.env` — كل الميزات بتتحكم فيها من هون بدون لمس الكود:

```ini
FEATURES_COURSES=1
FEATURES_CONTENT=1
FEATURES_NEWS=1
FEATURES_ASSIGNMENTS=1
FEATURES_DEADLINES=1
FEATURES_GRADES=1
FEATURES_NOTIFICATIONS=1
FEATURES_PER_COURSE_NOTIF=1
FEATURES_EMAIL_NOTIFICATIONS=1
FEATURES_AUTO_RELOGIN=1
FEATURES_RATE_LIMIT=1
FEATURES_ADMIN_PANEL=1
NOTIFICATION_CHECK_INTERVAL_MINUTES=20
```

5. **شغّل البوت**: في إعدادات Wispbyte، خليه يشغل الأمر:

```
python3 main.py
```

(لو السيرفر بيستخدم `python` بدل `python3`، غيّر الأمر من إعدادات السيرفر نفسها — ما بتحتاج تريمنال).

## ليش البوت رح يضل أونلاين؟

- لو قطع الاتصال بتليجرام أو Moodle أعطى خطأ مؤقت، البوت بيعيد المحاولة بعد 5 ثوانٍ تلقائياً.
- النسخة بتشتغل على **أي نسخة PTB** موجودة على السيرفر — ما في داعي لأي `pip install` أو تحديث.
- فحص الإشعارات الدورية (كل 20 دقيقة) بيشتغل على `APScheduler` الموجودة أصلاً بـ `requirements.txt`.

## أوامر الأدمن المتوفرة

`/start` `/login` `/logout` `/courses` `/content` `/news` `/assignments` `/deadlines` `/grades` `/prefs` `/togglecourse` `/email`
أدمن فقط: `/stats` `/ban` `/unban` `/user` `/logs` `/broadcast` `/userfind` `/tune` `/health`

## لو صار أي خطأ

افتح سجلات السيرفر (Console) وابحث عن كلمة `ERROR` — أي خطأ بظهر بصيغة:
`ERROR - توقف البوت بشكل غير متوقع: ... — إعادة التشغيل بعد 5 ثوانٍ`
والبوت رح يرجع يتصل لحاله بدون ما تلمس شي.
